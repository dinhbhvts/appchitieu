# Thu vien can thiet de CHAY THU o may ca nhan (dung SQLite).
# Dung dau ">=" (khong ghim cung 1 phien ban) de pip tu chon ban moi nhat
# tuong thich voi phien ban Python cua ban, ke ca Python 3.13 / 3.14.

# Web framework and ASGI server
fastapi>=0.115.5
uvicorn[standard]>=0.32.1

# ORM + migrations
SQLAlchemy>=2.0.36
alembic>=1.14.0

# Settings + validation
pydantic>=2.10
pydantic-settings>=2.6

# Testing
pytest>=8.3
PyJWT>=2.9
httpx>=0.28

# Reversible encryption for saved account passwords (So tay > Tai khoan)
cryptography>=43.0

# Lich am duong (quy doi ngay gio - dung cho sinh nhat/ngay gio am lich)
lunarcalendar>=0.0.9

# Upload file dinh kem (Ho so tien ich > Thong tin ca nhan) len Google Drive
# cua chu app, qua Service Account (khong can dang nhap OAuth tuong tac).
google-api-python-client>=2.150
google-auth>=2.35
python-multipart>=0.0.12

# GHI CHU: PostgreSQL (dung khi trien khai len may chu) can them driver.
# KHONG cai o buoc chay thu. Khi trien khai production moi chay:
#     python -m pip install -r requirements-prod.txt
